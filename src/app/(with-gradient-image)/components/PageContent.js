export default function PageContent({ children }) {
    return (
        <div style={{
            width: "50vw",
            marginLeft: "38vw",
            textAlign: "left"
        }}>{children}</div>
    );
}